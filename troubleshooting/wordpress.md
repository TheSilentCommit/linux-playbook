## 1. Caminho para checar os plugins instalados

On the directory /etc/www

```bash
wp plugin list
```

## 2. Checar instalação do wordpress

On the directory /etc/www It may ask to add the parameter --allow-root

```bash
wp core verify-checksums
```
